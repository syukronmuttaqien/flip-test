// List of all endpoint, we can manage all endpoint here, so it make developer easy to find endpoint url
import { BASE_URL } from './config';

export const API_TEST = {
  TRANSACTION_LIST: BASE_URL + '/frontend-test',
};
